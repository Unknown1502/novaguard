# NovaGuard Lambda Layer — Strands Agents SDK + AWS utilities
# Build: pip install -r requirements.txt -t python/
# Deploy: zip -r strands-agents-layer.zip python/ && aws lambda publish-layer-version ...

strands-agents>=0.1.0
boto3>=1.34.0
botocore>=1.34.0
aws-xray-sdk>=2.12.0
opensearch-py>=2.4.0
aws-lambda-powertools>=2.35.0
pydantic>=2.6.0
